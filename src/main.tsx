import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.tsx'
import './index.scss'

ReactDOM.createRoot(document.querySelector('.works')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
